dataname: human-ml-3d
_target_: model.data.humanml3d.HumanML3D

# Paths
datapath: ${path.datasets}/HumanML3D
splitpath: ${path.datasets}/humanml3d-splits

# Machine
batch_size: ${machine.batch_size}
num_workers: ${machine.num_workers}

# Motion
framerate: 20
max_duration: 500

downsample: false

# Other
tiny: false
progress_bar: true

data_augment: ${model.data_augment}
